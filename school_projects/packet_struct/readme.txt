How to run:
    python packet_struct.py name-of-cap-file.cap

Possibly unnecessary notes:
    1) name-of-cap-file: the name a captured file in the same folder as 
       packet_struct.py.
    2) the <.cap> is a must
    3) None cap files are not handled 